# Public runtime config. Only EXPO_PUBLIC_* vars reach the app bundle.
# NEVER put secrets here — the bundle is shipped to devices.
EXPO_PUBLIC_API_URL=https://api.ligo.app
EXPO_PUBLIC_USE_MOCKS=true
