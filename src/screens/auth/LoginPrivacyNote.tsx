import { LIText } from '@/components/ui';

export default function LoginPrivacyNote() {
  return (
    <LIText
      size="caption"
      color="muted"
      text="Your training data is never sold, and never shown to a coach without your permission."
      className="text-center"
    />
  );
}
