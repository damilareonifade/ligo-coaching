/**
 * Third-party brand colors, fixed by their owners' brand guidelines.
 *
 * These are NOT design tokens — never use them for Ligo's own UI. They live here
 * so no component file carries a raw hex value.
 */
export const googleLogoColors = {
  blue: '#4285F4',
  green: '#34A853',
  yellow: '#FBBC05',
  red: '#EA4335',
} as const;
